# Determines whether running programs can detect the shift key pressed with a mouse click.
mouse-shift-capture=true

# Sent clear followed by the enter key to the terminal.
# Used to clear scrollback when using Tmux
keybind = cmd+k=text:clear\u{20}\n
